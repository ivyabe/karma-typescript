function checkEnum(param: string) {
  if (param === "one") {
    return Numbers.One;
  } else if (param === "two") {
    return Numbers.Two;
  } else if (param === "three") {
    return Numbers.Three;
  } else if (param === "four") {
    return Numbers.Four;
  }
};